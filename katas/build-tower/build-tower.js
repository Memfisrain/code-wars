function buildTower(numberOfFloors) {
  /*var tower = Array.apply(null, new Array(nFloors));
  var width = nFloors + 2 * (nFloors - 1) - (nFloors > 1 ? 1 : 0);
  var lastRow = Array.apply(null, new Array(width)).map(function() {return '*';});
  
  tower.reduce(function(previousRow, value, index) {
    if (index) {
      previousRow[index - 1] = previousRow[width - index] = ' ';
    }
    
    tower[index] = previousRow.join('');
    
    return previousRow;
  }, lastRow);
  
  return tower.reverse();*/
}
